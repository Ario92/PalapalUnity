Version 0.0.6
    Bugfix: Fixed a bug in warning message color.
    bugfix: Fixed a bug in the validation system when use number after '_' in naming.
Version 0.0.5
    - Added: A new setting to validation folders.
    - Added: Validate SFXs.
    - Bugfix: Fixed a bug where the validation system would not recognize newly created folders.
    - Bugfix: Fixed a bug where '_' character in assets name would cause validation errors.
Version 0.0.4
    - Added: A settings window (palapal/Folder Validator Settings) to manually add and remove folders for validation.
    - Added: Automatic registration of newly created folder hierarchies into the validation system.

Version 0.0.3
    - Improved: Console warnings are now clickable, allowing quick navigation to the asset that violates the project's structure rules.